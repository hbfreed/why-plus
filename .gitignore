# --- Why+ : commit code, not data --------------------------------------------
# The Statcast pull (~3.5M rows/season) and the trained hand-off artifacts are
# large and regenerable. They belong on the training box, not in git.
data/
artifacts/
*.parquet
*.npy
*.pt

# pybaseball's own on-disk cache
.pybaseball/

# --- python / uv -------------------------------------------------------------
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.uv/
.python-version

# --- editors -----------------------------------------------------------------
.vscode/
.idea/
.DS_Store
